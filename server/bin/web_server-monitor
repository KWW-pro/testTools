#!/bin/bash -x
SLEEP_SEC=5
SHELL_PATH=$(dirname $0)
echo $SHELL_PATH
while((1));do
    $SHELL_PATH/status.sh
    rtn=$?
    if [ "$rtn"x == "-1"x ];then
        $SHELL_PATH/start.sh
    fi
    sleep $SLEEP_SEC
done
